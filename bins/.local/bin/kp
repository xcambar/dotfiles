#!/bin/sh

_DB=${KEEPASS_DATABASE:-${HOME}/keepass.kdbx}

read -sp "Enter your password:" password
tmp=`mktemp`
echo $password | keepassxc-cli ls -R -q ${_DB} > $tmp
entry=`cat $tmp | fzf | xargs`
echo $password | keepassxc-cli clip -q ${_DB} "${entry}"

rm $tmp
